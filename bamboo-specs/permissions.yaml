---
version: 2
deployment:
  name: dead-domains-linter - deploy
deployment-permissions:
  - groups:
      - extensions-developers
    permissions:
      - view
environment-permissions:
  - npmjs:
      - groups:
          - extensions-developers
        permissions:
          - view
          - deploy
